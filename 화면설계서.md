[설계서] 랭킹공장 - 화면 설계 및 시스템 명세

1. 화면별 상세 설계 (UI/UX Wireframe)

1.1 공통 레이아웃 (Global Layout)

PC: 좌측 고정 사이드바(Navigation) + 우측 메인 콘텐츠(Content Area).

Mobile: 상단 헤더(Sticky) + 메인 콘텐츠 + 하단 탭바(Bottom Tab).

Theme: CSS Variables 기반 Light Mode 기본 (#FBF8CC), Dark/Retro 확장 고려.

1.2 메인 페이지 (Dashboard) /

[구조: Bento Grid - 도시락통 배치]

헤더:

Logo(좌측, Black Han Sans), Search Bar(중앙, Cmd+K), Theme Toggle(우측).

Grid A (Hero Card - 2x2): [오늘의 빅매치]

좌우 분할 이미지 + 중앙 회전 VS 배지.

배경: 실시간 득표율 그래프 애니메이션.

Action: 카드 클릭 시 배틀 페이지 이동.

Grid B (Rank List - 1x2): [실시간 급상승]

1~5위 텍스트 리스트. 순위 숫자와 등락폭(🔺/🔻) 아이콘 강조.

Grid C (New Arrivals - 1x1): [AI 신규 생성]

NEW 스티커가 붙은 썸네일. "방금 생성됨" 타임스탬프.

1.3 배틀 페이지 (The Fight) /battle/[id]

[구조: 몰입형 전체 화면 (헤더/사이드바 숨김)]

Split View:

PC: 좌우 5:5 분할 / Mobile: 상하 5:5 분할.

중앙 절대 위치(Absolute)에 거대한 ⚡VS⚡ 아이콘.

Candidate Card:

이미지: object-cover로 영역 꽉 채움.

Interaction: Hover 시 확대(Scale 1.05), Click 시 폭죽+다음 매치 로딩.

Floating Action Bar (하단):

화면 하단 중앙 알약 모양 바.

[건너뛰기(⏭️)] [결과보기(🏆)] [댓글(💬)] 버튼.

1.4 랭킹 결과 페이지 (Leaderboard) /ranking/[id]

[구조: 포디움 + 리스트]

Podium (단상): 1등(왕관+금색), 2등(은색), 3등(동색) 높이가 다른 카드 배치.

List Table (4위~):

행 구성: 순위 | 변동 | 썸네일 | 이름 | 승률(Progress Bar) | ELO 점수.

Action: 행 클릭 시 상세 통계/댓글 모달.

Share Card: "내 취향 분석" 이미지 생성 버튼.

1.5 댓글 시스템 (Community Drawer)

[구조: 우측 슬라이드 서랍 (Drawer/Sheet)]

진입: 배틀/랭킹 페이지의 댓글 버튼 클릭.

작성: 랜덤 닉네임(수정 불가) + 50자 제한 텍스트 입력.

리스트: 지지하는 후보 태그 표시 + 좋아요(❤️) 기능.

2. 모바일 최적화 및 스타일링 명세

2.1 모바일 퍼스트 전략

Touch Target: 모든 버튼 최소 44px x 44px 영역 확보.

Safe Area: 아이폰 Home Indicator 영역 침범 방지 (padding-bottom).

Dynamic Viewport: 주소창 가림 방지를 위해 h-screen 대신 h-dvh 사용.

Gesture: 배틀 페이지에서 좌우 스와이프(Swipe) 투표 지원.

2.2 디자인 토큰 (CSS Variables)

:root {
--primary: #FFDE59; /_ 개나리색 _/
--accent: #FF5757; /_ 쨍한 레드 _/
--tertiary: #8C52FF; /_ 쨍한 퍼플 _/
--bg: #FBF8CC; /_ 크림색 배경 _/
--text: #000000; /_ 리얼 블랙 _/
--border-width: 3px; /_ 테두리 두께 _/
--shadow-offset: 4px; /_ 그림자 거리 _/
}

3. 시스템 아키텍처 및 데이터 로직

3.1 기술 스택 (Tech Stack)

Frontend: Next.js 15 (App Router), Tailwind CSS, shadcn/ui.

UI Library: Neobrutalism Components

역할: Neo-Brutalism 스타일의 버튼, 카드, 알림창 등 핵심 UI 컴포넌트 구현 시 활용.

참고: shadcn/ui 기반으로 커스텀된 라이브러리이므로 호환성 우수.

Database: Supabase (PostgreSQL + JSONB).

Logic: Supabase Edge Functions (ELO 점수 계산).

Automation: Vercel Cron + OpenAI GPT-4o-mini (콘텐츠 자동 생성).

3.2 데이터베이스 스키마 (DB Schema)

-- 1. 랭킹 주제 (Topics)
create table ranking_topics (
id uuid default gen_random_uuid() primary key,
title text not null,
category text, -- 'Food', 'Tech', 'Meme'
view_type text default 'BATTLE',
created_at timestamptz default now()
);

-- 2. 랭킹 후보 (Items)
create table ranking_items (
id uuid default gen_random_uuid() primary key,
topic_id uuid references ranking_topics(id) on delete cascade,
name text not null,
image_url text,
elo_score int default 1200, -- 핵심: ELO 점수
win_count int default 0,
loss_count int default 0,
match_count int default 0
);

-- 3. 댓글 (Comments)
create table comments (
id uuid default gen_random_uuid() primary key,
topic_id uuid references ranking_topics(id),
nickname text not null, -- 자동생성 (익명)
content text check (char_length(content) <= 100),
ip_hash text, -- 어뷰징 방지
created_at timestamptz default now()
);

3.3 핵심 비즈니스 로직

God Mode (관리자 도구):

/admin 접속 → 프롬프트 입력 → AI가 JSON 생성 → DB 적재.

ELO Rating Calculation (RPC):

투표 발생 시 클라이언트 계산 X -> vote_battle DB 함수 호출.

승자/패자 점수 차이에 따라 가중치(K-Factor) 적용하여 점수 갱신.

Auto-Generation (자동화):

Vercel Cron이 매일 아침 트렌드 키워드로 주제 생성 API 호출.
